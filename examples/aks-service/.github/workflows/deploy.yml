# =============================================================================
# Example: AKS cluster — Dev environment
# =============================================================================
# State key auto-derived:  platform/aks-cluster/dev/terraform.tfstate
# Var file auto-derived:   environments/dev.tfvars
# =============================================================================

name: "Deploy AKS - Dev"

on:
  push:
    branches: [main]
    paths: ["infra/**"]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  dev:
    uses: PixelTech-Solutions/Terraform/.github/workflows/terraform.yml@main
    with:
      working_directory: ./infra/aks-cluster
      environment: dev
      project_name: platform
      service_name: aks-cluster
    secrets: inherit
