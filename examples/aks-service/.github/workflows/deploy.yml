# =============================================================================
# Example: A team creating an AKS cluster
# =============================================================================
# This is ALL a team needs in their repo. Just import the template and set vars.
# The platform workflow handles init, plan, apply, destroy automatically.
#
# State key auto-derived:  <repo-name>/<environment>/terraform.tfstate
# Var file auto-derived:   environments/<environment>.tfvars
# =============================================================================

name: "Deploy AKS Cluster"

on:
  push:
    branches: [main]
    paths: ["infra/**"]
  pull_request:
    branches: [main]
    paths: ["infra/**"]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read
  pull-requests: write

jobs:
  dev:
    uses: PixelTech-Solutions/Terraform/.github/workflows/terraform.yml@main
    with:
      working_directory: ./infra
      environment: dev
    secrets: inherit

  qa:
    needs: [dev]
    if: github.ref == 'refs/heads/main'
    uses: PixelTech-Solutions/Terraform/.github/workflows/terraform.yml@main
    with:
      working_directory: ./infra
      environment: qa
    secrets: inherit

  uat:
    needs: [qa]
    if: github.ref == 'refs/heads/main'
    uses: PixelTech-Solutions/Terraform/.github/workflows/terraform.yml@main
    with:
      working_directory: ./infra
      environment: uat
    secrets: inherit

  prod:
    needs: [uat]
    if: github.ref == 'refs/heads/main'
    uses: PixelTech-Solutions/Terraform/.github/workflows/terraform.yml@main
    with:
      working_directory: ./infra
      environment: prod
    secrets: inherit

  demo:
    needs: [dev]
    if: github.ref == 'refs/heads/main'
    uses: PixelTech-Solutions/Terraform/.github/workflows/terraform.yml@main
    with:
      working_directory: ./infra
      environment: demo
    secrets: inherit
