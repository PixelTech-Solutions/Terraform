# =============================================================================
# Example: AKS cluster — UAT environment
# =============================================================================

name: "Deploy AKS - UAT"

on:
  push:
    branches: [main]
    paths: ["infra/**"]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  uat:
    uses: PixelTech-Solutions/Terraform/.github/workflows/terraform.yml@main
    with:
      working_directory: ./infra/aks-cluster
      environment: uat
      project_name: platform
      service_name: aks-cluster
    secrets: inherit
